🎨 src/styles/
Armazena a identidade visual do projeto em nível global. É onde você define como o site deve se comportar antes mesmo de qualquer componente carregar.

Conteúdo: Reset de CSS (normalize), variáveis de cores (tokens), tipografia global e animações reutilizáveis.